# Application configuration
APP_PORT=3001
LOG_LEVEL=INFO
# Comma-separated list of allowed origins for CORS. Use * to allow all (development).
CORS_ALLOW_ORIGINS=*

# Supabase configuration
# REQUIRED: Your Supabase project URL (from Project Settings -> API)
SUPABASE_URL=

# You should provide one of the following keys. Service role key is preferred for server-side use.
# If you set both, the app will prefer SUPABASE_SERVICE_ROLE_KEY.
# WARNING: Treat service role key as highly sensitive and NEVER expose it to browsers or client apps.
SUPABASE_SERVICE_ROLE_KEY=
# Alternatively, you can use the anon/public key for read-only or limited access use-cases.
SUPABASE_ANON_KEY=

# Optional: Target Postgres schema (defaults to "public")
SUPABASE_SCHEMA=public
