# FastAPI service
APP_PORT=3001
LOG_LEVEL=INFO
CORS_ALLOW_ORIGINS=*

# Supabase configuration
# Mandatory: Project URL and an active key. SUPABASE_KEY is the active key source and can be either service role or anon.
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
# Provide one of the following keys (prefer service role); map it into SUPABASE_KEY when deploying:
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
# Optional explicit schema
SUPABASE_SCHEMA=public
