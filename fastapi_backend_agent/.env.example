# FastAPI configuration
APP_PORT=3001
LOG_LEVEL=INFO
# Comma-separated list or "*" to allow all
CORS_ALLOW_ORIGINS=*

# Supabase configuration
# Required: your project URL, e.g. https://xyzcompany.supabase.co
SUPABASE_URL=
# Preferred: Service Role key (server-side only)
SUPABASE_SERVICE_ROLE_KEY=
# Optional alternative: Public anon key (less privileged)
SUPABASE_ANON_KEY=
# Backward-compat variable (deprecated): if used, app will still read it
SUPABASE_KEY=
# Optional: Target schema in your Postgres (default: public)
SUPABASE_SCHEMA=public
