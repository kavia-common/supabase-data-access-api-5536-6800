# Example environment configuration for FastAPI backend agent

# Application
APP_PORT=3001
LOG_LEVEL=INFO
# Comma-separated origins or * to allow all
CORS_ALLOW_ORIGINS=*

# Supabase configuration
# Required if using data endpoints; health/metrics do not require these to start the app.
SUPABASE_URL=https://your-project-ref.supabase.co
# Prefer service role; otherwise you may set SUPABASE_ANON_KEY. Legacy SUPABASE_KEY also supported.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SCHEMA=public
