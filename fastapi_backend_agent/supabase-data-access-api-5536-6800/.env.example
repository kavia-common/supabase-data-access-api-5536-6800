# Supabase Data Access API - .env.example
# Copy to .env and fill values. The application reads .env from the current working directory.

# --- Required ---
# Supabase project URL (e.g., https://YOUR-PROJECT-REF.supabase.co)
SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"

# Use the Service Role key for server-side (preferred). Do NOT expose this key to browsers/clients.
# Alternatively, you may use the anon/public key if that suits your security model.
SUPABASE_SERVICE_ROLE_KEY="YOUR-SERVICE-ROLE-KEY"
# SUPABASE_ANON_KEY="YOUR-ANON-KEY"

# --- Optional ---
# Target schema (default: public)
SUPABASE_SCHEMA="public"

# Comma-separated list of allowed origins for CORS. Use * for any origin.
# Examples:
# CORS_ALLOW_ORIGINS="*"
# CORS_ALLOW_ORIGINS="http://localhost:3000,https://example.com"
CORS_ALLOW_ORIGINS="*"

# Logging level: CRITICAL, ERROR, WARNING, INFO, DEBUG, TRACE, NOTSET (default: INFO)
LOG_LEVEL="INFO"

# Port to bind the FastAPI app (default: 3001)
APP_PORT="3001"

# Set to true/false to enable/disable auto-reload in development (uvicorn)
RELOAD="true"

# --- Legacy fallback (supported by code but discouraged) ---
# If neither SUPABASE_SERVICE_ROLE_KEY nor SUPABASE_ANON_KEY is set, the app will look at SUPABASE_KEY as legacy fallback.
# SUPABASE_KEY="YOUR-LEGACY-KEY"
